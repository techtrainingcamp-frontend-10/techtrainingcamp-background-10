import styled from 'styled-components'

export const CardWrapper = styled.div`
  .ant-card-actions {
    position: absolute;
    right: 0;
    bottom: 0;
  }

  .ant-tag {
    position:absolute;
    top: 10px;
    left: 10px;
  }
`
